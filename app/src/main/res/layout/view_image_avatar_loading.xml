<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="80dp"
    android:layout_height="80dp"
    android:clipChildren="true"
    android:clipToPadding="true">

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:cardBackgroundColor="@color/image_loading_base"
        app:cardCornerRadius="40dp"
        app:cardElevation="0dp">

        <ImageView
            android:id="@+id/imgPerfil"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/bg_image_loading_placeholder"
            android:contentDescription="@null"
            android:scaleType="centerCrop" />
    </androidx.cardview.widget.CardView>

    <com.facebook.shimmer.ShimmerFrameLayout
        android:id="@+id/shimmerContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone"
        app:shimmer_auto_start="false"
        app:shimmer_base_alpha="0.9"
        app:shimmer_base_color="@color/image_loading_base"
        app:shimmer_direction="left_to_right"
        app:shimmer_duration="1100"
        app:shimmer_highlight_alpha="0.55"
        app:shimmer_highlight_color="@color/katzen_verde_claro"
        app:shimmer_repeat_count="-1"
        app:shimmer_shape="radial">

        <View
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/bg_image_loading_placeholder" />
    </com.facebook.shimmer.ShimmerFrameLayout>

    <ProgressBar
        android:id="@+id/progressImageLoading"
        style="?android:attr/progressBarStyleSmall"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_gravity="center"
        android:elevation="4dp"
        android:indeterminate="true"
        android:indeterminateTint="@color/katzen_rosa"
        android:visibility="gone" />

</FrameLayout>
